/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
var removeElement = function(nums, val) {
  if (!nums || !val || nums.length === 0) return [];
};

module.exports = removeElement;
