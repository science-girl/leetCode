/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
const twoSum = (nums, target) => {
  if (!nums || target < 0 || nums.length === 0) return [];
};

module.exports = twoSum;
